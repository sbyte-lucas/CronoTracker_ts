// Prisma Config - Generator & Datasource
// Este arquivo contém apenas as configurações base do Prisma

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["prismaSchemaFolder"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
